import { TranslocoHttpLoader } from './transloco-http-loader';

describe('TranslocoHttpLoader', () => {
  it('should create an instance', () => {
    expect(TranslocoHttpLoader).toBeTruthy();
  });
});
