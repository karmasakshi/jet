import { AvailableFont } from '@jet/types/available-font.type';
import { DEFAULT_LANGUAGE_OPTION } from './default-language-option.constant';

export const DEFAULT_FONT: AvailableFont = DEFAULT_LANGUAGE_OPTION.font;
