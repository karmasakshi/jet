import { AvailableTheme } from '@jet/types/available-theme.type';
import { DEFAULT_THEME_OPTION } from './default-theme-option.constant';

export const DEFAULT_THEME: AvailableTheme = DEFAULT_THEME_OPTION.value;
